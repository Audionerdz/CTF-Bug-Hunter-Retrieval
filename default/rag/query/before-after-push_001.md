---
chunk_id: rag::query::before-after-push::001
domain: rag
chunk_type: technique
---

# Query: Antes y Después de Push

## Antes de Hacer Push
```bash
query "qué hacer antes de cambios"
query "flujo trabajo git completo"
query "git pull antes de empezar"
```

## Haciendo el Push
```bash
query "cómo subir a github"
query "git push origin main"
query "subir cambios remoto"
```

## Después de Push (Arrepentimiento)
```bash
query "empujé a github error"
query "revert commit pushed"
query "deshacer cambios remoto"
```
